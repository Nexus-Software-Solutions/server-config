# server-config-nexus

<br />

```bash
sudo apt update
sudo apt install wget

# prints Hello world on the terminal
wget -O - https://raw.githubusercontent.com/Nexus-Software-Solutions/server-config/main/hello.sh | bash

# runs the automated script
wget -O - https://raw.githubusercontent.com/Nexus-Software-Solutions/server-config/main/automated_first.sh | bash

# this script will download the script as a text file
# run this step by step manually
# DO NOT RUN IT AS A WHOLE IN ONE GO
wget https://raw.githubusercontent.com/Nexus-Software-Solutions/server-config/main/manual_last.sh -O manual_last.txt
```
